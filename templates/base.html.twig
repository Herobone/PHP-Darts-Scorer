<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Darts Scorer{% endblock %}</title>
    {% if hmrClient != "" %}
        <script type="module" src="{{ hmrClient }}"></script>
    {% endif %}
    {% for css in styleAssets %}
        <link rel="stylesheet" href="{{ css }}">
    {% endfor %}
    <style>
        /* Add any specific custom styles here */
        body {
            font-family: 'Inter', sans-serif; /* Using Inter font, similar to Flowbite */
        }
    </style>
</head>
<body class="flex flex-col min-h-screen bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-white">

<header class="fixed top-0 left-0 z-50 w-full bg-white border-b border-gray-200 dark:bg-gray-800 dark:border-gray-700 shadow-md">
    {% include "navbar.html.twig" %}
</header>

{# Main Content Area - Padding top to account for fixed header #}
<main class="flex-1 container mx-auto mt-20 p-4">
    {% block body %}
    {% endblock %}
</main>

{# Footer #}
<footer class="bg-gray-200 dark:bg-gray-700 p-4 text-center mt-8">
    <p class="text-gray-700 dark:text-gray-300">&copy; {{ "now"|date("Y") }} Darts Scorer - Julius G.</p>
</footer>

<script type="module" src="{{ assets }}" defer></script>

</body>
</html>
